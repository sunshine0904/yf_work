head.h 是pcap文件头及数据包头相关。
10.pcap是源文件包含10个数据包。
output.txt是stage1文件夹代码生成的加密结构，此文件只包含一段加密结构代码，长度538字节。
mod10.pcap是增加完加密结构后生成的文件，包含加密结构。此文件用wireshark打不开是因为wireshark不能解析自定义的加密结构。在stage3文件夹中的代码可以解析此数据包。

stage2文件夹下程序功能：在10.pcap文件中的10段数据中插入output.txt中的加密结构，生成包含加密结构的文件mod10.pcap。
编译命令：gcc pcap.c -w -o stage2
执行命令：./stage2
执行后的打印结果，只打印了部分字段，生成的mod10.pcap即为目标文件。


