head.h是pcap包头及数据头相关。
struct_head.c struct_head.h文件是加密结构相关。
mod10.pcap是stage2生成的包含由加密数据段的pcpa包。
file_len.c show_pcap.c仅作为调试时使用。

编译命令:gcc stage3.c aes_lib.c aes_lib.h -w -o stage3
执行命令:./stage3
打印结果只打印出部分字段，加密结构只打印出长度字段，其它字段也可以打印。


